# Copiar para .env.local

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Apenas server-side (Server Actions / Route Handlers). NUNCA prefixar
# com NEXT_PUBLIC_ — esta chave ignora RLS.
SUPABASE_SERVICE_ROLE_KEY=
